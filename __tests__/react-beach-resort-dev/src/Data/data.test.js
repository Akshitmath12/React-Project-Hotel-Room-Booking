 Test generated by RoostGPT for test ReactTest1 using AI Type Open AI and AI Model gpt-4-1106-preview

// Import the data from the data.js file
const data = require('./path_to_data.js');

describe('Room Data', () => {
  test('data is an array', () => {
    expect(Array.isArray(data)).toBeTruthy();
  });

  test('every item has a sys object with an id', () => {
    data.forEach(item => {
      expect(item).toHaveProperty('sys');
      expect(item.sys).toHaveProperty('id');
      expect(typeof item.sys.id).toBe('string');
    });
  });

  test('required fields are present in each room', () => {
    const requiredFields = ['name', 'slug', 'type', 'price', 'size', 'capacity', 'pets', 'breakfast', 'featured', 'description'];
    data.forEach(item => {
      requiredFields.forEach(field => {
        expect(item.fields).toHaveProperty(field);
      });
    });
  });

  // Add more test cases as needed...
});

