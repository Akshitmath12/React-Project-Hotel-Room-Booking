 Test generated by RoostGPT for test ReactTest1 using AI Type Open AI and AI Model gpt-4-1106-preview

// data.test.js
import data from './data';

describe('data.js tests', () => {
  
  test('data array is not empty', () => {
    expect(data).toBeDefined();
    expect(Array.isArray(data)).toBe(true);
    expect(data.length).toBeGreaterThan(0);
  });

  test('each data item has all required fields', () => {
    const requiredFields = ['sys', 'fields'];
    data.forEach(item => {
      requiredFields.forEach(field => {
        expect(item[field]).toBeDefined();
      });
      // You could add more detailed checks here, such as ensuring `fields` contains `name`, `slug`, etc.
      const itemFields = ['name', 'slug', 'type', 'price', 'size', 'capacity', 'pets', 'breakfast', 'featured', 'description', 'extras', 'images'];
      itemFields.forEach(field => {
        expect(item.fields[field]).toBeDefined();
      });
    });
  });

});


