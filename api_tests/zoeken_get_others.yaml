# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /kvk/zoeken_get for http method type GET in artillery framework

# RoostTestHash=2acea70323


# Artillery YAML script for testing non-success API responses
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 5
      arrivalRate: 2
  ensure:
    p99: 200
  http:
    pool: 10
  plugins:
    expect: {}
  payload:
    path: "zoeken_get_others.csv"
    fields:
      - "kvkNummer"
      - "vestigingsnummer"
      - "handelsnaam"
    skipHeader: true

scenarios:
  - name: "Testing non-success API responses"
    flow:
      # Invalid URL request
      - get:
          url: "/invalid_url"
          expect:
            - statusCode: 404 
          think: 1

      # Invalid kvkNummer 
      - get:
          url: "/kvk/zoeken?kvkNummer={{ kvkNummer }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: items
          think: 1

      # Missing authorization token
      - get:
          url: "/kvk/zoeken?vestigingsnummer={{ vestigingsnummer }}"
          expect:
            - statusCode: 401
            - hasProperty: items
          think: 1

      # Unauthorized access (incorrect token)
      - get:
          url: "/kvk/zoeken?handelsnaam={{ handelsnaam }}"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 403
            - hasProperty: items
          think: 1
