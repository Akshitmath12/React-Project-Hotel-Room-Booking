# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /accounts_post for http method type POST in artillery framework

# RoostTestHash=53e96f9805


config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  plugins:
    expect: {}

  payload:
    path: "accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true

scenarios:
  - name: Non-Success API Response Test
    flow:
      # Request: POST /accounts - Invalid Payload
      - post:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            email: "not-valid-email-format"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals:
                "message": "Invalid email format"
          think: 3

      # Request: POST /accounts - No Token
      - delete:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
          think: 3

      # Request: POST /accounts - Invalid Token
      - post:
          url: "/accounts"
          headers:
            Authorization: "Bearer invalid_token"
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
          think: 3

      # Request: POST /accounts - Invalid URL
      - post:
          url: "/wrong-url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
          think: 3

      # Request: POST /accounts - server error 500
      - get:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 500
          think: 3
