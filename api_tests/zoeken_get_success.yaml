# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /kvk/zoeken_get for http method type GET in artillery framework

# RoostTestHash=2acea70323


config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "zoeken_get_success.csv"
    fields:
      - kvkNummer
      - vestigingsnummer
      - handelsnaam
    skipHeaders: true

scenarios:
  - name: "[Example Scenario] KV Zoeken API"
    flow:
      # Test '/kvk/zoeken' with valid kvkNummer, vestigingsnummer and handelsnaam
      - get:
          url: "/kvk/zoeken?kvkNummer={{ kvkNummer }}&vestigingsnummer={{ vestigingsnummer }}&handelsnaam={{ handelsnaam }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
      - think: 1
