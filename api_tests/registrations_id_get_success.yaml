# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /registrations/{id}_get for http method type GET in artillery framework

# RoostTestHash=f67303cb4b


# Artillery YAML test script for API endpoint "/registrations/{id}"
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  # Payload ordering & path
  payload:
    path: registrations_id_get_success.csv
    fields: ["id"]
    skipHeader: true
    order: sequence
  headers:
    Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Scenario-1: GET /registrations/{id}"
    flow:
      - get:  # GET request to fetch an item
          url: "/registrations/{{ id }}"
          expect:
            - statusCode: 200 
            - hasProperty: "id"
            - hasProperty: "reference"
            - hasProperty: "startDate"
            - hasProperty: "endDate"
            - hasProperty: "status"
            - hasProperty: "credentialUrl"
            - hasProperty: "suspendedUntil"
            - hasProperty: "issuedAt"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasProperty: "recipient"
      - think: 5 # think time after each request
