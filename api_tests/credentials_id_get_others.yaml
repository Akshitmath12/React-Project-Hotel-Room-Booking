# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials/{id}_get for http method type GET in artillery framework

# RoostTestHash=c02d3561ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "credentials_id_get_others.csv"
    fields: [ "id" ]
    skipHeader: true

scenarios:
  - name: "Testing non-success cases for /credentials/{id} API"
    flow:
      # Request with incorrect URL
      - get:
          url: "/credntl/{{ id }}"
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          think: 1

      # Request with empty id path parameter
      - get:
          url: "/credentials/"
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          think: 1

      # Request with invalid id path parameter
      - get: 
          url: "/credentials/invalid-id"
          headers:
            Token: {{$processEnvironment.AUTH_TOKEN}}
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          think: 1

      # Request without authentication
      - get:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - contentType: "application/json"
          think: 1
