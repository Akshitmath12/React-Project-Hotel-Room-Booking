# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons/{id}/registrations_post for http method type POST in artillery framework

# RoostTestHash=0973c499e9


# Artillery test script for testing various non-success Http response codes
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_post_others.csv"
    fields:
      - "reference"
      - "startDate"
      - "endDate"
      - "credentialUrl"
  plugins:
    expect: {}

scenarios:
  - name: "Non-success API Responses"
    flow:
      - # Test request with empty payload
        post:
          url: "/recipients/persons/{{ id }}/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1

      - # Test request with invalid id
        post:
          url: "/recipients/persons/invalid_id/registrations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1

      - # Test request with incorrect endpoint
        get:
          url: "/recipients/persons/{{ id }}/invalid_path"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1

      - # Test request without required authorization headers
        post:
          url: "/recipients/persons/{{ id }}/registrations"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1

      - # Test request with invalid token
        post:
          url: "/recipients/persons/{{ id }}/registrations"
          headers:
            Token: "invalid_token"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 403
            - hasHeader: "content-type"
            - contentType: "application/json"
        think: 1
