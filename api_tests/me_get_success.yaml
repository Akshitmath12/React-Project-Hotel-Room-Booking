# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_get_success.csv"
    fields:
      ["id", "did", "name", "image", "email", "settings", "createdAt", "updatedAt"]
    skipHeader: true
  headers:
    Token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Validate successful API requests"
    flow:
      # Test API request 1: Get my account details
      - get:
          url: "/me"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "createdAt"
            - hasProperty: "did"
            - hasProperty: "email"
            - hasProperty: "id"
            - hasProperty: "image"
            - hasProperty: "name"
            - hasProperty: "settings"
            - hasProperty: "updatedAt"
          afterResponse: "think"
