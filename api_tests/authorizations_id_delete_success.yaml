# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  defaults:
    headers:
      Token: "{{ $processEnvironment.AUTH_TOKEN }}"
      
scenarios:
  - name: "Test authorization deletion API"
    flow:
      - get:
        # Fetch authorization by ID
          url: "/authorizations/{{ id }}"
          expect:
            - statusCode: 200
            - hasProperty: api-version
            - equals:
                - "api-version"
                - "1.0.0"
      - think: 1

      - delete:
        # Delete authorization
          url: "/authorizations/{{ id }}"
          expect:
            - statusCode: 200
            - hasProperty: api-version
            - equals:
                - "api-version"
                - "1.0.0"
      - think: 1

      - get:
        # Check that authorization is deleted
          url: "/authorizations/{{ id }}"
          expect:
            - statusCode: 404
      - think: 1
