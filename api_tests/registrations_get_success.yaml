# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /registrations_get for http method type GET in artillery framework

# RoostTestHash=5a542f8139


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 300
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "registrations_get_success.csv"
    fields:
      - "id"
      - "reference"
      - "startDate"
      - "endDate"
      - "status"
      - "credentialUrl"
      - "suspendedUntil"
      - "issuedAt"
      - "createdAt"
      - "updatedAt"
      - "recipient"
    skipHeader: true

scenarios:
  - name: "Registrations API test"
    flow:
      # Request 1: Get all Registrations
      - get:
          url: "/registrations?size={{ size }}&offset={{ offset }}&recipient-id={{ recipientId }}&credential-url={{ credentialUrl }}"
          headers:
            "Token": "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: 'api-version'   
            - hasHeader: 'page-size'   
            - hasHeader: 'page-offset'
            - hasProperty: 'total'
            - hasProperty: 'registrations'
            - equals: 'items[0].id' 
            - equals: 'items[0].reference'
        think: 5
