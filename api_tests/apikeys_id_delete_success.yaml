# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=4affc26394


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 20
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "apikeys_id_delete_success.csv"
    fields: ["id"]
    options:
      skipHeader: true
      
scenarios:
  - name: Test successful API behavior with valid payloads
    flow:
      # Request to delete API key
      - post:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
          think: 2
