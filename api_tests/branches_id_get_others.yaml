# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework

# RoostTestHash=061355b093


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload: 
    path: "branches_id_get_others.csv"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Testing Non-Success API Responses"
    flow:
      # Testing with an incorrect URL 
      - get: 
          url: "/recipients/branches/incorrect_id"
          # Not Authorized - No Authorization header provided
          headers: {}
          # Expecting a 404 response 
          expect:
            - statusCode: 404
        think: 1
      
      # Testing with an incorrect URL and Auth Token included
      - get:
          url: "/recipients/branches/incorect_id"
          # Auth Token included but still API request should throw
          headers:
            [[$processEnvironment.AUTH_TOKEN]]: "invalid_auth_token"
          # Expecting a 404 response 
          expect:
            - statusCode: 404
        think: 1
    
      # Correct URL but Not Authorized - No Authorization header provided
      - get:
          url: "/recipients/branches/{{ id }}"
          headers: {}
          # Expecting a 401 response 
          expect:
            - statusCode: 401
        think: 1

      # Authorized but using an incorrect ID
      - get:
          url: "/recipients/branches/incorrect_id"
          headers: 
            [[$processEnvironment.AUTH_TOKEN]]: "{{$processEnvironment.AUTH_TOKEN}}"
          # Expecting a 404 response 
          expect:
            - statusCode: 404
        think: 1

      # Testing the correct use case
      - get:
          url: "/recipients/branches/{{ id }}"
          headers: 
            [[$processEnvironment.AUTH_TOKEN]]: "{{$processEnvironment.AUTH_TOKEN}}"
          # Expecting a 200 response 
          expect:
            - statusCode: 200
        think: 1
