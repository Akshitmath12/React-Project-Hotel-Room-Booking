# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations_get for http method type GET in artillery framework

# RoostTestHash=3b64f22044


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    - expect
  payload:
    path: "authorizations_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
  http:
    pool: 10
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Testing Non-Success API requests"
    flow:
      # Sending a GET request without authorization
      - get:
          url: "/authorizations"
          expect:
            - statusCode: 401 # expecting an unauthorized status code
      - think: 5

      # Sending a GET request with an invalid authorization
      - get:
          url: "/authorizations"
          headers:
            Token: "invalid_token"
          expect:
            - statusCode: 401 # Expecting unauthorized status code as token is invalid
      - think: 5     

      # Sending a GET request to a non-existent url
      - get:
          url: "/nonexistenturl"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expecting NotFound status code as url is invalid
      - think: 5      

      # Sending a GET request with correct token but incorrect payload
      - get:
          url: "/authorizations?username=incorrectusername"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Expecting NotFound status code as username payload is incorrect
      - think: 5

      # Sending a GET request with correct token and payload
      - get:
          url: "/authorizations?username={{ username }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "username"
      - think: 5
