# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /logout_post for http method type POST in artillery framework

# RoostTestHash=64d33906e5


config: 
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  defaults:
    headers:
      Token: "{{$processEnvironment.AUTH_TOKEN}}"
  plugins:
    expect: {}
  payload:
    path: "./logout_post_success.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true

scenarios:
  - name: "API request scenarios"
    flow:
      # Scenario 1: Logout API test
      - post: 
          url: "/logout"
          headers:
            api-version: "1.0.0"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: { "api-version": '1.0.0' }
          beforeRequest: 
            - "myCustomProcessor"
        think: 5
