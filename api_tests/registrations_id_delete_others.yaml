# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /registrations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=2c75b43156


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  ensure:
    maxErrorRate: 0
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_id_delete_others.csv"
    skipHeader: true
    fields:
      - "id"

scenarios:
  - name: "non-successful API request scenario"
    flow:
      # Test DELETE with invalid authorization
      - think: 3
      - delete:
          url: "/registrations/{{ id }}"
          headers:
            Token: "IncorrectToken"
          expect:
            - statusCode: 401
      # Test DELETE with invalid id
      - think: 3
      - delete:
          url: "/registrations/invalid-id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      # Test DELETE with missing authorization
      - think: 3
      - delete:
          url: "/registrations/{{ id }}"
          expect:
            - statusCode: 401
