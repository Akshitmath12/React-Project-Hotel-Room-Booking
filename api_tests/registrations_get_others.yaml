# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /registrations_get for http method type GET in artillery framework

# RoostTestHash=5a542f8139


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "recipient-id"
      - "credential-url"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 1
      name: "Load test simulation"
  defaults:
    headers:
      Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Failing API requests"
    flow:
      # Test case: Invalid payload
      - post:
          url: "/registrations"
          json:
            recipient-id: "9999"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "status"
          think: 1

      # Test case: Unauthenticated request
      - get:
          url: "/registrations"
          headers:
            Token: "Invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - hasProperty: "status"
          think: 1

      # Test case: Invalid URL
      - get:
          url: "/wrong_url"
          expect:
            - statusCode: 404
          think: 1

      # Test case: Missing required field
      - post:
          url: "/registrations"
          json:
            credential-url: null
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "status"
          think: 1

      # Test case: Incorrect payload field name
      - post:
          url: "/registrations"
          json:
            wrong_field_name: "sample_string"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "status"
          think: 1
