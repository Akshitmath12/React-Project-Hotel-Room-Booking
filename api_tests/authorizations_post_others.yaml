# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations_post for http method type POST in artillery framework

# RoostTestHash=ed8c1b7bff


config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    - name: dev
      target: "{{ $processEnvironment.API_HOST }}"
      securitySchema:
        - type: apiKey
          name: Token
          in: header
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "authorizations_post_others.csv"
    fields:
      - email
    skipHeader: true

scenarios:
  - name: 'Invalid request tests for /authorizations endpoint'
    flow:
      - post:
          url: "/authorizations"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - contentType: "application/json"
            - hasProperty: "email"
            - hasProperty: "detail"
            - hasProperty: "status"
          think: 2

      - log: 'Invalid URL case'
      - post:
          url: "/invalid_url"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect: 
            - notStatusCode: 200
            - statusCode: 404
          think: 2
