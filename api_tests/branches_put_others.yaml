# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches_put for http method type PUT in artillery framework

# RoostTestHash=63322ffb26


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "branches_put_others.csv"
    fields: ["cocNumber", "cocBranchNumber", "city", "tradeName", "phone", "email"]
    skipHeader: true

scenarios:
  - name: "Artillery test for non successful API responses"
    flow:
      # First Request - Test without Authorization Token
      - post:
          url: "/recipients/branches"
          headers:
            Content-type: "application/json"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - bodyContains:
               - message: "Authorization token missing"
        think: 7

      # Second Request - Test with incorrect cocNumber
      - post:
          url: "/recipients/branches"
          headers:
            Content-type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            cocNumber: "Invalid"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - bodyContains:
                - message: "Invalid cocNumber entered"
        think: 7

      # Third Request - Test with invalid email
      - post:
          url: "/recipients/branches"
          headers:
            Content-type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "Invalid email"
          expect:
            - statusCode: 400
            - bodyContains:
                - message: "Invalid email format"
        think: 7

      # Fourth Request - Test with missing fields
      - post:
          url: "/recipients/branches"
          headers:
            Content-type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - bodyContains:
                - message: "Required fields are missing"
        think: 7
