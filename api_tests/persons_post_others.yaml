# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons_post for http method type POST in artillery framework

# RoostTestHash=b4614c0581


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "persons_post_others.csv"
    fields:
      - "birthDate"
      - "initials"
      - "lastName"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: Invalid Requests Scenario
    flow:
      - post:
          url: "/recipients/persons"
          headers:
            Content-Type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            birthDate: "{{ birthDate }}"
            initials: "{{ initials }}"
            lastName: "{{ lastName }}"
          expect:
            - statusCode: 400
        think: 1

      - post:
          url: "/recipients/persons"
          headers:
            Content-Type: "application/json"
          json:
            birthDate: "{{ birthDate }}"
            initials: "{{ initials }}"
            lastName: "{{ lastName }}"
          expect:
            - statusCode: 401
        think: 1

      - post:
          url: "/invalidPersonsEndpoint"
          headers:
            Content-Type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            birthDate: "{{ birthDate }}"
            initials: "{{ initials }}"
            lastName: "{{ lastName }}"
          expect:
            - statusCode: 404
        think: 1

      - post:
          url: "/recipients/persons"
          headers:
            Content-Type: "application/json"
            Token: "Invalid_Token"
          json:
            birthDate: "{{ birthDate }}"
            initials: "{{ initials }}"
            lastName: "{{ lastName }}"
          expect:
            - statusCode: 401
        think: 1

      - post:
          url: "/recipients/persons"
          headers:
            Content-Type: "application/json"
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            initials: "{{ initials }}"
            lastName: "{{ lastName }}"
          expect:
            - statusCode: 400
            - hasProperty: "errors.birthDate"
