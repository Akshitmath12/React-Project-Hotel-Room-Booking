# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /registrations/{id}_patch for http method type PATCH in artillery framework

# RoostTestHash=3512d7d6f0


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_id_patch_success.csv"
    fields:
      - "id"
      - "reference"
      - "revoked"
      - "suspendedUntil"
      - "endDate"
      - "startDate"
    skipHeader: true
  headers:
    Token: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Test registration API"
    flow:
      # PATCH request to update registration details
      - patch:
          url: "/registrations/{{ id }}"
          json:
            reference: "{{ reference }}"
            revoked: "{{ revoked }}"
            suspendedUntil: "{{ suspendedUntil }}"
            endDate: "{{ endDate }}"
            startDate: "{{ startDate }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasHeader: "api-version"
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "id"
            - hasProperty: "reference"
            - hasProperty: "startDate"
            - hasProperty: "endDate"
            - hasProperty: "status"
            - hasProperty: "credentialUrl"
            - hasProperty: "suspendedUntil"
            - hasProperty: "issuedAt"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasProperty: "recipient"
      # A delay to simulate user thinking time
      - think: 5
