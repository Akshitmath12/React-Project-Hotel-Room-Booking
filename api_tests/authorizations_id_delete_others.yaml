# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=cb128291e8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields: ["id"]
    skipHeader: true
scenarios:
  - name: "Remove Authorized Account - API Error Cases"
    flow:
      # Trying to delete with invalid id
      - post:
          url: "/authorizations/invalidID"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
          think: 2
      # Unauthorized - Token header not provided
      - post:
          url: "/authorizations/{{ id }}"
          expect:
            - statusCode: 401
          think: 2
        # Trying to delete with invalid Token 
      - post:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "INVALID_TOKEN"
          expect:
            - statusCode: 403
          think: 2
      # hitting to wrong endpoint
      - get:
          url: "/wrongEndpoint/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 2
 