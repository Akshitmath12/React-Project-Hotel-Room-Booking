# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons_get for http method type GET in artillery framework

# RoostTestHash=aa0a5d4980


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "persons_get_success.csv"
    fields:
      - "size"
      - "offset"
      - "name"
      - "birthDate"
  headers:
    Content-Type: "application/json"
    Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Testing /recipients/persons API endpoint"
    flow:
      # First test request to get the list of persons
      - get:
          url: "/recipients/persons?size={{ size }}&offset={{ offset }}&name={{ name }}&birth-date={{ birthDate }}"
          # We expect a 2xx response from the server and a JSON response body
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
            - hasProperty: ["persons", "total"]
            - equals: [{"total": "{{ $response.body.length }}"}, {"persons": "{{ $response.body.items }}"},]
        think: 1
