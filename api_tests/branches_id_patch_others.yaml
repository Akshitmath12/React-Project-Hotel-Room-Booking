# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_patch for http method type PATCH in artillery framework

# RoostTestHash=9caaa2ef96


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {} 
  phases: 
    - duration: 60 
      arrivalRate: 20
  payload:
    path: "./branches_id_patch_others.csv"
    fields: 
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
scenarios:
  - name: "Error Handling Test Scenario"
    flow:
    # Test: Invalid Trade Name
      - post:
          url: "/recipients/branches/{id}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 422
            - hasProperty: "validation-error"
          think: 1 

    # Test: Missing required field tradeName from the payload
      - post:
          url: "/recipients/branches/{id}"
          json:
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 422
            - hasProperty: "validation-error"
          think: 1 

    # Test: Invalid email format in the request payload
      - post:
          url: "/recipients/branches/{id}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "invalid-email-format"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 422
            - hasProperty: "validation-error"
          think: 1 

    # Test: Invalid URL
      - post:
          url: "/recipients/incorrect_endpoint/{id}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
          think: 1 

    # Test: Unauthorized request (missing Token)
      - post:
          url: "/recipients/branches/{id}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 401
          think: 1
