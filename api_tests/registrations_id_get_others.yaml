# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /registrations/{id}_get for http method type GET in artillery framework

# RoostTestHash=f67303cb4b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"
  processor: "./artillery-processor-id-generation.js"
  plugins:
    expect: {}
  payload:
    path: "registrations_id_get_others.csv"
    fields: ["id"]
    skipHeader: true
scenarios:
  - flow:
      - log: "Testing with invalid Token"
        get:
          url: "/registrations/{{ id }}"
          headers:
            Token: "INVALID_TOKEN"
          expect:
            - statusCode: 401
        think: 1
      - log: "Testing with no Token"
        get:
          url: "/registrations/{{ id }}"
          headers:
            Token: ""
          expect:
            - statusCode: 403
        think: 1
      - log: "Testing with non-existing registration id"
        get:
          url: "/registrations/1234567890"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Testing with malformed registration URL"
        get:
          url: "/registrations1234567890"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
        think: 1
