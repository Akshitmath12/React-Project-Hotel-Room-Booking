# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /login_post for http method type POST in artillery framework

# RoostTestHash=e0cf5b2e0e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "login_post_others.csv"
    fields:
      - email
      - otp
      - client
    skipHeader: true
  headers:
    Content-Type: "application/json"
    Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-successful API request test scenario"
    flow:
      # Post request with missing required fields in payload to login endpoint
      - post:
          url: "/login"
          json:
            email: "{{ email }}"
            otp: ""
            client: "{{ client }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "message"
          think: 1

      # Post request with invalid email format in payload to login endpoint
      - post:
          url: "/login"
          json:
            email: "invalid email format"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "message"
          think: 1

      # Post request with incorrect API/token in headers to login endpoint
      - post:
          url: "/login"
          headers:
            Token: "incorrect API token"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "message"
          think: 1

      # Post request to invalid URL/endpoint
      - post:
          url: "/invalid_url"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "message"
          think: 1
