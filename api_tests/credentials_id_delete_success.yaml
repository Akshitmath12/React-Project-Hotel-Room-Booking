# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=f1fc1d06f7


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
    - duration: 120
      arrivalRate: 10
  plugins:
    expect: {}
  payloads:
    - name: "id"
      path: "credentials_id_delete_success.csv"
      skipHeader: true

scenarios:
  - name: "Delete a credential from my Wallet"
    flow:
      # Request: Deleting a credential from my Wallet
      - post:
          url: "/credentials/{{id}}"
          headers: 
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect: 
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: 
                - "api-version": "1.0.0"
            - hasProperty: ["description"]      
      - think: 2
