# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons/{id}_get for http method type GET in artillery framework

# RoostTestHash=131d25110f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "persons_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Non-success API responses scenario"
    flow:
      # Invalid or missing Token causing Unauthorized request
      - get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Token: "invalid_token_here"
          expect:
            - statusCode: 401
        think: 1

      # Valid Token but incorrect URL
      - get:
          url: "/recipients/unknown/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      # Valid Token and URL but ID does not exist in the system
      - get:
          url: "/recipients/persons/nonexistent_id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1

      # Access to an ID not allowed for current authenticated user
      - get:
          url: "/recipients/persons/id_of_other_user"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 403
        think: 1
