# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons/{id}/registrations_post for http method type POST in artillery framework

# RoostTestHash=0973c499e9


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "registrations_post_success.csv"
    fields:
      - "id"
      - "reference"
      - "startDate"
      - "endDate"
      - "credentialUrl"
    skipHeader: true

scenarios:
  - name: "Test new registrations with API"
    flow:
      # POST new registration with valid data
      - post:
          url: "/recipients/persons/{{ id }}/registrations"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            reference: "{{reference}}"
            startDate: "{{startDate}}"
            endDate: "{{endDate}}"
            credentialUrl: "{{credentialUrl}}"
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasProperty: "reference"
            - hasProperty: "startDate"
            - hasProperty: "endDate"
            - hasProperty: "status"
            - hasProperty: "credentialUrl"
            - hasProperty: "suspendedUntil"
            - hasProperty: "issuedAt"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"               
          think: 1
