# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=4affc26394


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "../payload/apikeys_id_delete_others.csv"
    fields:
      - "id"
      - "authentication"
    skipHeader: true

scenarios:
  - name: "Test case for non-successful API requests"
    flow:
      # Testing a GET request with a non-existent id
      - get:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
          think: 1 

      # Testing a POST request without authentication
      - post:
          url: "/apikeys"
          json:
            id: "{{ id }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
          think: 1

      # Testing a POST request with invalid header
      - post:
          url: "/apikeys"
          json:
            id: "{{ id }}"
          headers:
            Token: "INVALID_TOKEN"
          expect:
            - statusCode: 401
            - hasProperty: "message"
          think: 1

      # Testing a DELETE request without proper authentication
      - delete:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "INVALID_TOKEN"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - headerEquals: ["Content-Type", "application/json"]
            - hasProperty: "message"
          think: 1

      # Testing a DELETE request with non-existent id
      - delete:
          url: "/apikeys/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: ["Content-Type", "application/json"]
            - hasProperty: "message"
          think: 1
