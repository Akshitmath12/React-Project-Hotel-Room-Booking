# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /apikeys_post for http method type POST in artillery framework

# RoostTestHash=28a54f1579


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "apikeys_post_others.csv"
    fields:
      - "Token"
    skipHeader: true

scenarios:
  - name: Non-Successful API requests tests
    flow:
      # Testing incorrect API key
      - post:
          url: "/apikeys"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            apikey: "InvalidApiKeyHere"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: ["message", "Invalid ApiKey provided"]
        think: 1
      # Testing invalid URL
      - post:
          url: "/invlaidUrl"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            apikey: "{{ apikey }}"
          expect:
            - statusCode: 404
        think: 1
      # Testing without ApiKey in header
      - post:
          url: "/apikeys"
          json:
            apikey: "{{ apikey }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: ["message", "No ApiKey provided in the headers"]
        think: 1
