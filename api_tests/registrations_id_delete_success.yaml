# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /registrations/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=2c75b43156


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 360
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "registrations_id_delete_success.csv"
    fields: ["id"]
    skipHeader: true

scenarios:
  - name: 'Testing API endpoint /registrations/{id}'
    flow:

    # First, it performs a GET request with valid UUID in path parameter
    - get:
        url: "/registrations/{{ id }}"
        headers:
          Token: "{{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 200
          - hasHeader: "api-version"
          - equals: ["headers.api-version", "1.0.0"]

    # Adding a think time of 1 second
    - think: 1

    # Then, it performs a DELETE request with valid UUID in path parameter
    - delete:
        url: "/registrations/{{ id }}"
        headers:
          Token: "{{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 202

    # Adding a think time of 1 second
    - think: 1
