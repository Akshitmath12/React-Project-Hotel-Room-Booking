# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /accounts_post for http method type POST in artillery framework

# RoostTestHash=53e96f9805


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 300
      arrivalRate: 50
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
  plugins:
    expect: {}

scenarios:
  - name: "Create new account and validate API response"
    flow:
      - post:
          # Create an account using valid email
          url: "/accounts"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          json:
            email: "{{$payload.email}}"
          expect:
            - statusCode: 201
          think: 1
