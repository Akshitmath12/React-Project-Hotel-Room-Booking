# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches_get for http method type GET in artillery framework

# RoostTestHash=74a40da00e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "branches_get_success.csv"
    fields: ["coc-number", "coc-branch-number", "trade-name", "size", "offset"]
    skipHeader: true
  plugins:
    expect: {}
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: Validate /recipients API endpoint
    flow:
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}&coc-branch-number={{ coc-branch-number }}&trade-name={{ trade-name }}&size={{ size }}&offset={{ offset }}"
          expect:
            - statusCode: 200
            - hasProperty: "branches"
            - hasProperty: "total"
          beforeRequest:
            - "log"
          afterResponse: "think"
