# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=1ce4bf6e3c


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}
    
  # Enable payload from CSV file
  payload:
    path: "branches_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
    order: sequence

scenarios:
  - name: "Scenario for Testing Non-Successful API Responses"
    flow:
      
     # Invalid Header expects "401 Unauthorized" status code
      - log: "Performing DELETE /recipients/branches/{id} with Invalid Header"
        delete:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "InvalidToken" 
        expect:
          - statusCode: 401
      
      # Added 'think' after each request
      - think: 2
      
      # Missing required header expects "400 Bad Request" status code
      - log: "Performing DELETE /recipients/branches/{id} with missing required header"
        delete:
          url: "/recipients/branches/{{ id }}"
        expect:
          - statusCode: 400
          
      # Added 'think' after each request
      - think: 2
          
      # Invalid URL expects "404 NotFound" status code
      - log: "Performing DELETE on invalid URL"
        delete:
          url: "/recipients/branches/invalidurl"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404

      # Added 'think' after each request
      - think: 2      
      
      # Missing required path parameter expects "400 Bad Request" status code
      - log: "Performing DELETE /recipients/branches/ without required path parameter"
        delete:
          url: "/recipients/branches/"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
