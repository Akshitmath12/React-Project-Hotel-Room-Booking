# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons_get for http method type GET in artillery framework

# RoostTestHash=aa0a5d4980


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "persons_get_others.csv"
    fields:
      - "name"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: 'Scenario for Testing Non-Successful API Responses'
    flow:
      
      - get:
          url: "/recipients/persons"  # Fail case - Without any query parameters
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400  # Expect HTTP 400 status code for invalid request
            - hasProperty: 'error'

      - think: 2   # Pause for 2 seconds

      - get:
          url: "/recipients/someInvalidEndpoint"  # Fail case - Invalid Endpoint
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404  # Expect HTTP 404 status code for invalid URL
            - hasProperty: 'error'

      - think: 2
      
      - get:
          url: "/recipients/persons?name={{ name }}" # Fail case - filter with invalid name from CSV
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 
            - hasProperty: 'error'     

      - think: 2      

      - get:
          url: "/recipients/persons?size=51"  # Fail case - Size limit exceeded
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400                  
            - hasProperty: 'error'
            
      - think: 2
          
      - get:
          url: "/recipients/persons?offset=-1"  # Fail case - Negative offset
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'

      - think: 2

      - get:
          url: "/recipients/persons?birth-date=abcd"  # Fail case - Invalid Date format
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
