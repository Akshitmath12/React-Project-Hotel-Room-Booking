# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/persons/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=819791060f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "persons_id_delete_others.csv"
    fields:
      - "id"
  processor: "./my-processor.js"
  scenarios:
    - name: "Test for Non-success API responses"
      flow:
        # Test Case 1: Without authentication
        - log: "Test Case 1. No Authentication"
        - get: 
            url: "/recipients/persons/{{ id }}"
          think: 3
        - log: "Expecting UnAuthorized 401 status"
        - expect:
            - statusCode: 401 # Expecting Unauthorized status code
        - think: 1

        # Test Case 2: With Invalid token 
        - log: "Test Case 2: Invalid Token in header"
        - get: 
            url: "/recipients/persons/{{ id }}"
            headers:
              Token: "InvalidTokenHere"
          think: 3
        - log: "Expecting UnAuthorized 401 status"
        - expect:
            - statusCode: 401 # Expecting Unauthorized status code
        - think: 1

        # Test Case 3: Missing required parameter in the URL
        - log: "Test Case 3: Missing required parameter in the URL"
        - get: 
            url: "/recipients/persons/"
            headers:
              Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          think: 3
        - log: "Expect 404 - Not Found status"
        - expect:
            - statusCode: 404 # Expecting 'Resource not found' status code

        # Test Case 4: Non-Existing ID in URL parameter
        - log: "Test Case 4: Non-Existing ID in URL parameter"
        - get: 
            url: "/recipients/persons/non_existing_id"
            headers:
              Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          think: 3
        - log: "Expect 404 - Not Found status"
        - expect:
            - statusCode: 404 # Expecting 'Resource not found' status code
