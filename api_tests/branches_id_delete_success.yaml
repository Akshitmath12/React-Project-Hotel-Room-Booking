# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=1ce4bf6e3c


config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  phases:
    - duration: 300
      arrivalRate: 50
  plugins:
    expect: {}
  payload:
    path: "branches_id_delete_success.csv"
    fields:
      - id
    skipHeader: true

scenarios:
  - name: "API test Scenario"
    flow:
      - comment: "GET request for /recipients/branches/{id}"
        get:
          url: "/recipient/branches/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: 
                header: "api-version"
                value: "1.0.0"
        think: 1
