# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /grants_get for http method type GET in artillery framework

# RoostTestHash=692c6b853f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "grants_get_success.csv"
    fields:
      - "id"
      - "account"
      - "createdAt"
      - "updatedAt"
      - "token"
    skipHeader: true
  headers:
    Token: "{{$processEnvironment.AUTH_TOKEN}}"
scenarios:
  - name: "Test Scenario 1"
    flow:
      - get: 
          url: "/grants"
          # This is a successful request for retrieving account grants.
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: ["grants", "total"]
            - hasHeader: ["api-version"]
            # The successful response will include an account object with id, image, name and email properties.
            - hasProperty: ["grants.items.account", "grants.items.account.id", "grants.items.account.image", "grants.items.account.name", "grants.items.account.email"]
            # The successful response will also include an account object with id, token, createdAt, updatedAt.
            - hasProperty: ["grants.items.id", "grants.items.token", "grants.items.createdAt", "grants.items.updatedAt"]
      - think: 1
