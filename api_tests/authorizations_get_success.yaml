# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /authorizations_get for http method type GET in artillery framework

# RoostTestHash=3b64f22044


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_get_success.csv"
    fields:
      - "id"
    skipHeader: true
  environments:
    development:
      variables:
        AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test Scenario: Successful API behavior with valid input payloads"
    flow:
     - get:
         # Request GET /authorizations
         url: "/authorizations"
         # Headers configuration
         headers:
           Token: "{{ $processEnvironment.AUTH_TOKEN }}"
           api-version: "1.0.0"
         #Expected Outcomes for GET request
         expect:
           - statusCode: 200
           - hasHeader: "api-version"
           - equals:
               - "api-version"
               - "1.0.0"
           - hasProperty: "total"
           - hasProperty: "authorizations"
           - hasProperty: ["authorizations", 0, "id"]
           - hasProperty: ["authorizations", 0, "name"]
           - hasProperty: ["authorizations", 0, "email"]
           - hasProperty: ["authorizations", 0, "updatedAt"]
           - hasProperty: ["authorizations", 0, "createdAt"]
     - think: 1
