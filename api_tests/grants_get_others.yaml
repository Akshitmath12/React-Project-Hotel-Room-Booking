# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /grants_get for http method type GET in artillery framework

# RoostTestHash=692c6b853f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 300
      arrivalRate: 50
  plugins:
    expect: {}
  payload:
    path: "grants_get_others.csv"
    fields:
      - "email"
      - "password"

    skipHeader: true

scenarios:
  - name: "Test for non-success API responses"
    flow:
      # Test with missing authorization header
      - log: "Sending request without authorization header"
        get:
          url: "/grants"
        expect:
          - statusCode: 401
        think: 5

      # Test with invalid authorization token
      - log: "Sending request with invalid authorization token"
        get:
          url: "/grants"
          headers:
            Token: "InvalidToken"
        expect:
          - statusCode: 403
        think: 5

      # Test with incorrect URL
      - log: "Sending request to incorrect URL"
        get:
          url: "/invalid_url"
        expect:
          - statusCode: 404
        think: 5

      # Test with valid authorization but invalid payload
      - log: "Sending request with valid authorization but invalid payload"
        post:
          url: "/grants"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            password: "invalid_password"
        expect:
          - statusCode: 400
        think: 5

 # Test with completely invalid request
      - log: "Sending completely invalid request"
        patch:
          url: "/grants/not_existing"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalid_field: "invalid_value"
        expect:
          - statusCode: 405
        think: 5

