# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework

# RoostTestHash=061355b093


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payloads:
    - name: "branches_id_get_success.csv"
      path: "payloads/branches_id_get_success.csv"
      fields: ["id"]
      skipHeader: true
scenarios:
  - name: "Get Recipient branch"
    flow:
      # Get branch by ID
      - get:  
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}" 
          expect:
            - statusCode: 200 
            # Headers validation
            - hasHeader: "api-version"
              headerEquals: 
                "api-version": "1.0.0"
            # Response body validation  
            - hasProperty: "id"
            - hasProperty: "cocBranchNumber"
            - hasProperty: "cocNumber"
            - hasProperty: "createdAt"
            - hasProperty: "deregistrationDate"
            - hasProperty: "eoriNumber"
            - hasProperty: "tradeName"
            - hasProperty: "tradeNames"
            - hasProperty: "updatedAt"
            # Matching property format
            - matchesRegexp:
                "id": "/\b[0-9a-f]{8}\b-\b[0-9a-f]{4}\b-\b[0-9a-f]{4}\b-\b[0-9a-f]{4}\b-\b[0-9a-f]{12}\b/"
                "cocNumber": "/^\\d{8}$/"
                "cocBranchNumber": "/^\\d{12}$/"
                "eoriNumber": "/^NL\\d{9}$/"
      - think: 1
