# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /recipients/branches/{id}_patch for http method type PATCH in artillery framework

# RoostTestHash=9caaa2ef96


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payloads:
    - path: "branches_id_patch_success.csv"
      fields: ["id", "tradeName", "phone", "email"]
      order: sequence
      skipHeader: true

scenarios:
  - name: "Test /recipients/branches/{id} API"
    flow:
      - post:
          url: "/recipients/branches/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          beforeRequest: $http.printRequest
          beforeResponse: $http.printResponse
          expect:
          - statusCode: 200
          - hasProperty: "id"
          - hasProperty: "tradeName"
          - hasProperty: "phone"
          - hasProperty: "email"
          - hasProperty: "cocNumber"
          - hasProperty: "cocBranchNumber"
          - hasProperty: "tradeNames"
          - hasProperty: "eoriNumber"
          - hasProperty: "deregistrationDate"
          - hasProperty: "createdAt"
          - hasProperty: "updatedAt"
          - hasHeader: "api-version"
          - headerEquals: ["api-version", "1.0.0"]
      - think: 1
