# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework

# RoostTestHash=f1fc1d06f7


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 1
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "./credentials_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "Non-Success API Response Scenario"
    flow:
      - post:
          url: "/non/existent/path"
          expect:
            - statusCode: 404
          think: 1
      # Requesting with an invalid UUID in the API path
      - delete:
          url: "/credentials/not-a-valid-uuid"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
          think: 1 
      # Requesting with a correct UUID but wrong token
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Token: "Not a valid token"
          expect:
            - statusCode: 401
          think: 1 
      # Requesting with a correct UUID and correct token
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: 'content-type'
            - headerEquals: ['content-type', 'application/json']
            - hasProperty: 'message'
            - equals: ['-1', 'Credential has been successfully deleted.']
          think: 1 
