# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /logout_post for http method type POST in artillery framework

# RoostTestHash=64d33906e5


config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p99: 500
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "logout_post_others.csv"
    fields:
      - "UserName"
      - "Password"
    skipHeader: true

scenarios:
  - name: "Non-successful API requests"
    flow:
      # Trying to access with missing token in header
      - post:
          url: "/logout"
          headers:
            Content-Type: "application/json"
          body: 
            "UserName": "{{ UserName }}"
            "Password": "{{ Password }}"
          expect: 
            - statusCode: 401
            - hasProperty: 'error'
            # Assert error message
            - equals:
                - "message.error"
                - "Access token missing in header"
          think: 1
      # Trying to access with wrong token in header
      - post:
          url: "/logout"
          headers:
            Content-Type: "application/json"
            Token: "WrongToken"
          body: 
            "UserName": "{{ UserName }}"
            "Password": "{{ Password }}"
          expect: 
            - statusCode: 403
            - hasProperty: 'error'
            # Assert error message
            - equals:
                - "message.error"
                - "Access token is incorrect"
          think: 1
      # Trying to access with (correct token, but wrong URL)
      - post:
          url: "/wrongurl"
          headers:
            Content-Type: "application/json"
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          body: 
            "UserName": "{{ UserName }}"
            "Password": "{{ Password }}"
          expect: 
            - statusCode: 404
            - hasProperty: 'error'
            # Assert error message
            - equals:
                - "message.error"
                - "URL Not found"

