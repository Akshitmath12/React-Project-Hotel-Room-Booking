# Test generated by RoostGPT for test PostTest using AI Type Azure Open AI and AI Model roost-gpt4-32k


# Test generated for /errors_get for http method type GET in artillery framework

# RoostTestHash=588ae64bad


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "errors_get_success.csv"
    skipHeader: true
    fields:
      - "field1"
      - "field2"
  plugins:
    expect: {}

scenarios:
  - name: "Test Errors API"
    flow:
    - get:
        url: "/errors"
        headers:
          Token: "{{$processEnvironment.AUTH_TOKEN}}"
        beforeRequest: "setUpHeaders"
        expect:
          - statusCode: 200
          - hasProperty: "items"
          - hasProperty: "items[0].error"
          - hasProperty: "items[0].description"
        capture:
          - json: "$.items[0]"
            as: "item"
    - think: 1
